import { NewsOrgDirective } from './news-org.directive';

describe('NewsOrgDirective', () => {
  it('should create an instance', () => {
    const directive = new NewsOrgDirective();
    expect(directive).toBeTruthy();
  });
});
